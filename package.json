{
  "name": "soap-shop",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "node-sass": "^4.14.1"
  },
  "scripts": {
    "build": "node-sass --output public/css --include-path scss scss/main.scss",
    "build:watch": "yarn build --watch",
    "start": "npx serve public",
    "stylelint": "npx stylelint '**/*.css'",
    "stylelint:fix": "npx stylelint '**/*.css' --fix",
    "fmt": "prettier --write '**/*.{html,css,js}'"
  },
  "dependencies": {
    "intersection-observer": "^0.11.0",
    "jquery": "^3.5.1",
    "loading-attribute-polyfill": "^1.5.4",
    "lozad": "^1.15.0"
  }
}
